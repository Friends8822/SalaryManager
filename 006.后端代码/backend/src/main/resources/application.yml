server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/salary_manager?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: zaiont
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

app:
  jwt:
    secret: salary-manager-spring-boot-jwt-secret-key-2026
    expiration-ms: 86400000
